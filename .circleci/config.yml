version: 2

jobs:
  build:
    working_directory: ~/test
    docker:
      - image: condaforge/linux-anvil
    branches:
      only:
        - master
    steps:
      - run:
          command: bash .CI/circleci_build.sh
