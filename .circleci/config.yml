version: 2

jobs:
  build:
    docker:
      - image: condaforge/linux-anvil
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          command: ./.CI/circleci_build.sh
